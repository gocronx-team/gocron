# 【开发】环境变量

# 应用部署基础路径（如部署在子目录 /admin，则设置为 /admin/）
VITE_BASE_URL = /

# API 请求基础路径（开发环境设置为 / 使用代理，生产环境设置为完整后端地址）
VITE_API_URL = /

# 代理目标地址：指向 gocron 后端（air / go run 默认跑 5920）
VITE_API_PROXY_URL = http://localhost:5920

# Delete console
VITE_DROP_CONSOLE = false